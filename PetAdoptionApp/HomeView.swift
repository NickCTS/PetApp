import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Welcome to Pet Adoption App")
                .font(.largeTitle)
                .padding()
            
            Text("Find your perfect companion!")
                .font(.headline)
                .padding()
        }
    }
}
